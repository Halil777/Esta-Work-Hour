# =============================================
# Copy this file to .env and fill in values:
#   cp .env.example .env
#   nano .env
# =============================================
  cd /home/Esta-Work-Hour
  docker compose build backend tenant-admin
  docker compose up -d backend tenant-admin

  docker compose build backend --no-cache && docker compose up -d backend


# Database
DB_NAME=workhour
DB_USERNAME=postgres
DB_PASSWORD=change_this_strong_password

# Backend
ADMIN_TOKEN=workhour-admin-2025
MAIL_USER=your_gmail@gmail.com
MAIL_PASS=your_gmail_app_password

# Super Admin
SUPER_ADMIN_USERNAME=superadmin
SUPER_ADMIN_PASSWORD=change_this_super_password
SUPER_ADMIN_JWT_SECRET=change_this_to_a_long_random_string_min_32_chars
